# Query
query user($id: ID!) {
    user(id: $id) {
        firstName
        lastName
        email
        password
    }
}

query session {
    session
}

# Mutation

mutation login {
    startSession
}
mutation logout {
    endSession
}
